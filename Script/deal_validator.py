# Proprietary — not included in this public repository.
#
# Implements the main deal validation pipeline: each CSFloat listing passes
# through sequential filters covering price percentile ranking, collection age,
# sales recency, sticker noise, float-range competition, EMA trend direction,
# and tier-based profit thresholds.
